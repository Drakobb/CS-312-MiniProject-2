<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= cocktail.strDrink %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="display-4 text-center"><%= cocktail.strDrink %></h1>
        <div class="row justify-content-center mt-4">
            <img class="img-fluid" src="<%= cocktail.strDrinkThumb %>" alt="Cocktail Image" style="max-width: 300px;">
        </div>

        <div class="mt-4">
            <h2>Ingredients:</h2>
            <ul class="list-group">
                <% for (let i = 1; i <= 15; i++) { %>
                    <% if (cocktail[`strIngredient${i}`]) { %>
                        <li class="list-group-item"><%= cocktail[`strIngredient${i}`] %>: <%= cocktail[`strMeasure${i}`] || 'to taste' %></li>
                    <% } %>
                <% } %>
            </ul>
        </div>

        <div class="mt-4">
            <h2>Instructions:</h2>
            <p class="lead"><%= cocktail.strInstructions %></p>
        </div>

        <a href="/" class="btn btn-secondary mt-4">Go Back</a>
    </div>
</body>
</html>
